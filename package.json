{
  "name": "codex-meetup-jkt-jun-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "build": "vite build && mkdir -p dist/assets && cp -R assets/. dist/assets/",
    "preview": "vite preview --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
